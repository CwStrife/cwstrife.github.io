// LTC Fish Browser — Staff configuration
// Edit these values then refresh the kiosk. Do not commit production PINs to a public repo.
window.LTC_STAFF_CONFIG = {
  defaultPin: '1234',
  maxFailedAttempts: 5,
  lockoutSeconds: 60
};
